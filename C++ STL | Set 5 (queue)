link = https://practice.geeksforgeeks.org/problems/c-stl-set-5-queue/1?page=1&difficulty[]=-1&difficulty[]=0&category[]=Queue&sortBy=submissions

void push(queue<int> &q,int x)
{
   q.push(x);
}


/*pop out the front element 
from the queue q and returns it */
int pop(queue<int> &q)
{
    if(!q.empty()){
        int ans = q.front();
        q.pop();
         return ans; 
    }
    return -1;
  
}

/*returns the size of the queue q */
int getSize(queue<int> &q)
{
    return q.size();
}

/*returns the last element of the queue */
int getBack(queue<int> &q)
{
    if(!q.empty()){
        return q.back();
    }
   return -1;
}

/*returns the first element of the queue */
int getFront(queue<int> &q)
{
    if(!q.empty()){
        return q.front();
    }
   return -1;
}
