class Solution
{
    public:
    //Function to count the number of substrings that start and end with 1.
    long binarySubstring(int n, string a){
        
       int counts = count(a.begin(),a.end(),'1');
       return counts*(counts-1)/2;
        // Your code here
        
    }

};
