string convert(string s)
{
    string s1 = "";
    int temp;
    for(int i=0;i<s.length();i++)
    {
        if(s[i]>=97&&s[i]<=122)
        {
            s1.push_back(122-(s[i]-97));
        }
        else
        {
            s1.push_back(90-(s[i]-65));
        }
    }
    return s1;
    // code here.
}
